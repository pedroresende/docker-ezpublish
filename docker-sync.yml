version: "2"

syncs:
    db-sync:
        src: './mysql'
        dest: '/var/lib/mysql'
    ezpublish-sync:
        src: './ezpublish'
        dest: '/var/www/html/ezpublish'
    nginx-sync:
        src: './logs'
        dest: '/var/log/nginx'
